const prompt = require("prompt-sync")({ sigint: true });
console.clear();

//Finonacci arrays
/*
function fibonacci(x){
    let c = -1, d = -2 ;
    let arrayfibona=[0, 1];
    for (let b=0; b<=x; b++){
        factor = factor-b+factor-b-1;
        if (factor<1){
            factor= factor*(-1)
        }else{};
        arrayfibona.push(factor);  
        }
        console.log(factor);
         
    arrayfibona.push(factor);
    console.log(arrayfibona);
    }
fibonacci(20)

// Promedio
let nota = 0;
let suma = 0;
let cuantos = 0;
nota = parseInt(prompt ('Ingrese nota:'));
do{      
        suma += nota;
        cuantos ++;
        nota = parseInt(prompt ('Ingrese nota:'));
}while (nota != 0)
console.log(suma);
console.log(cuantos);
console.log(suma/cuantos);

// pruebas

let a = Math.random();
console.log(math.floor(a*10));
console.log(math.round(a*10));
console.log(math.tofixed(2)((a*10));


//Nombre

let nombre = prompt('Ingrese su nombre: ');
console.log(`Hola ${nombre} !`);

// anio nacimiento

let anionac = parseInt(prompt ('ingree anio de nacimiento: '));
Edad = 2022 - anionac;
console.log(`Tienes ${Edad} anios`);


// numeros pares
function pares(){
    for (let x=0; x<=100; x++)
    if (x%2 == 0 ){
        console.log(x);
    }
}
pares()

// pseudocodigo
let numero = 0
function pseudo(){
    let numero = parseInt(prompt (`ingrese el numero: `));
    if (numero > 500){
        console.log(numero * 0.18);
    }
}
pseudo();
*/


